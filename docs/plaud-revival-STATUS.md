# Plaud 자동화 되살리기 — 완료 현황 (2026-07-03)

## ✅ 한 일
1. **원인 진단**: 옛 봇은 PC + 만료되는 web.plaud.ai 세션에 의존 → 6/17경 중단.
2. **연결 방식 확정**: Plaud 공식 MCP(`https://mcp.plaud.ai/mcp`)를 이 저장소 `.mcp.json`으로 연결.
   → API 키·구글 서비스계정 불필요. Claude가 직접 읽고, 이미 연결된 Google Calendar에 등록.
3. **백필 완료 (밀린 5/21~7/3)** — 사장님 캘린더(ksnap87@gmail.com)에 등록:
   - **회의 이벤트 38건** (`[Plaud] …`, 회색, 실제 시각 KST)
   - **할일(📌) 23건** (빨강, 하루 전 알림) — 최근 회의 4건의 전체 액션아이템
     - 07-03 구독포인트·결합할인(7) / 07-03 구독·단품할인(3) / 07-03 구독모델전환(3) / 07-02 가족그룹(10)
   - 형식은 2026-05-19 배치와 동일(`plaud_id`/`plaud_todo` 중복방지키 포함).

## ⏳ 남은 것 (사장님 확인/선택)
- **최근 회의 8건(7/1×5·6/30×2·6/29)의 할일**: 백필 도중 Plaud MCP가 재인증을 요구해(비대화형 세션 한계)
  노트를 못 읽어 **회의 이벤트만** 등록됨. 재인증 후 `/plaud-sync` 한 번이면 할일까지 채워짐.
- **과거 회의(5/21~6/10)의 할일**: 마감이 이미 지나 소음이 되므로 **의도적으로 제외**(회의 기록만 등록).
  원하시면 추가 가능.
- **제외 항목**: 개인 통화·짧은 음성메모·수초짜리 잡음·"How to use Plaud".

## ▶ 앞으로 돌리는 법
- 어느 Claude Code 세션에서든 **`/plaud-sync`** 실행 → 새 녹음을 읽어 회의·할일 자동 등록(중복 방지).
- **매일 자동**: Plaud MCP는 가끔 대화형 재인증이 필요해 완전 무인은 불안정. 권장:
  1. (간단) 아침에 `/plaud-sync` 한 번 — 30초.
  2. (반자동) Claude Code "루틴"으로 `/plaud-sync` 매일 예약 — 재인증 필요할 때만 개입.
  3. (완전 무인) `supabase/functions/plaud-sync/`(PR #1)에 Plaud 개발자 API 연결 — 서버가 대신 실행.

## 되돌리기
- 모든 이벤트 description에 `plaud_id=`/`plaud_todo=` 키가 있어, 특정 회의/할일만 골라 삭제 가능.
